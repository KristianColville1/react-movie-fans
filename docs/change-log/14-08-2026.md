started the new characteristics branch off develop

pagination on discover, upcoming and actors, tmdb caps you at 500 pages so the control does too

dropped the refetch interval, it was re-polling every six minutes and hiding the fact that react query caches at all, going back a page is free now

pages were reading filter values by index, moved them to by name before adding any more or they all shift

multi criteria search, genre checkboxes instead of the single menu, release year from and to, minimum rating, and a clear all

genre is an or not an and, ticking two widens the list
