supabase client in, the env vars were named for next not vite so nothing was reaching the app

auth context holds the session and listens for changes

sign in and sign up on the one page, email and password

favourites, fantasy movies and the review pages are private routes now, everything else stays public

header shows who is signed in and hides the private nav when youre not

filtering and sorting are for signed in users, anyone else gets told to sign in

turned off email confirmation in supabase, otherwise a new account cant sign in until a mail arrives and the free tier only sends a few an hour
