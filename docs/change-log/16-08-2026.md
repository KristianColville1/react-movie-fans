moved the stores over to supabase, fantasy movies favourites and favourite actors all write to the database now

the context loads whatever belongs to the signed in user and clears it on sign out

favourite actors done, heart on the actor card and a page of its own, actor card got an action slot like the movie card has

set the supabase folder up properly, the schema is declarative files and a migration in the repo now instead of sql pasted into the dashboard

five end to end tests, had to use the playwright library directly because the test runner doesnt work in this setup

pushed the schema up and checked a second account cant see the first ones rows
