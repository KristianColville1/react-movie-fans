cut the deploy branch off develop

dockerfile builds the app and serves the static files with nginx, two stages so the node stuff doesnt end up in the image

nginx needs the spa fallback or refreshing a deep link like /movies/123 gives a 404, caught that before it went out

github action builds the image and pushes it to ghcr on every push to main and develop

looked up how bunny redeploys, it does not watch the registry, the action has to tell it to roll onto the new image, so we tag by commit sha instead of latest

ran the end to end tests against the container itself rather than the dev server, all five pass

still need the bunny app id and api key in the repo settings before redeploys turn on
