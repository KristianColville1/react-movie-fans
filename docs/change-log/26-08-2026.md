container workflow tidied up and the vite env goes in as build args, without them the bundle ships empty, app is live on bunny now

renamed the site to moviefans, the tab still said vite + react + ts and the header said tmdb client

dropped the tagline out of the header, it was eating the whole bar

header arrows page the list now, you dont have to scroll to the bottom to move on

site has a fixed width so it stops stretching on big screens, five cards a row instead of six

trailer on the movie page above the overview, the extra posters moved into a carousel underneath

profile page for the display name and password, avatar menu in the header instead of the email sitting in the nav

the filters were only narrowing the twenty movies already on screen, they go to tmdb now so the whole catalogue gets filtered and the page counts mean something
