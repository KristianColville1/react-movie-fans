container workflow tidied up and the vite env goes in as build args, without them the bundle ships empty, app is live on bunny now

renamed the site to moviefans, the tab still said vite + react + ts and the header said tmdb client

dropped the tagline on mobile, it was eating the whole bar
